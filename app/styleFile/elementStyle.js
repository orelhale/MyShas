import { StyleSheet } from "react-native";

export const elementStyle = StyleSheet.create({
   element: {

   },
   item: {
      marginBottom: 10,
      backgroundColor: "red",
      fontSize: 30,
      flexBasis: "30%",
      justifyContent: 'center',
      alignItems: "center",
      height:60,
      // width:60,
      
   },
});